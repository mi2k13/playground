import React from 'react';
import SearchForm from './SearchForm';

const SearchShow = () => (
  <div>
    <h1>Search show</h1>

    <SearchForm placeholder="Breakind Bad, Lost,..." />
  </div>
);

export default SearchShow;
